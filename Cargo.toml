[package]
name = "sure-unpack"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Universal archive unpacker that auto-detects format and calls the right tool"

[[bin]]
name = "unpack"
path = "src/main.rs"

[dependencies]
